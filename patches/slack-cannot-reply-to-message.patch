diff --git a/plugins/platforms/slack/adapter.py b/plugins/platforms/slack/adapter.py
index 05aeaf56f..03bd43e5c 100644
--- a/plugins/platforms/slack/adapter.py
+++ b/plugins/platforms/slack/adapter.py
@@ -21,6 +21,7 @@ from typing import Dict, Optional, Any, Tuple, List
 try:
     from slack_bolt.async_app import AsyncApp
     from slack_bolt.adapter.socket_mode.async_handler import AsyncSocketModeHandler
+    from slack_sdk.errors import SlackApiError
     from slack_sdk.web.async_client import AsyncWebClient
     import aiohttp
 
@@ -30,6 +31,7 @@ except ImportError:
     AsyncApp = Any
     AsyncSocketModeHandler = Any
     AsyncWebClient = Any
+    SlackApiError = Exception  # type: ignore[misc, assignment]
 
 import sys
 from pathlib import Path as _Path
@@ -1407,7 +1409,27 @@ class SlackAdapter(BasePlatformAdapter):
                     if broadcast and i == 0:
                         kwargs["reply_broadcast"] = True
 
-                last_result = await self._get_client(chat_id).chat_postMessage(**kwargs)
+                # LOCAL MODIFICATION (patches/slack-cannot-reply-to-message.patch):
+                # if Slack rejects the thread parent (`cannot_reply_to_message`),
+                # retry the same chunk as a plain channel message.
+                try:
+                    last_result = await self._get_client(chat_id).chat_postMessage(**kwargs)
+                except SlackApiError as _slack_err:
+                    err_payload = getattr(_slack_err, "response", None) or {}
+                    err_code = err_payload.get("error") if isinstance(err_payload, dict) else None
+                    if err_code == "cannot_reply_to_message" and "thread_ts" in kwargs:
+                        logger.warning(
+                            "[Slack] cannot_reply_to_message — thread parent ts %s "
+                            "rejected; retrying chunk %d as channel-level message",
+                            kwargs.get("thread_ts"), i,
+                        )
+                        kwargs.pop("thread_ts", None)
+                        kwargs.pop("reply_broadcast", None)
+                        # Disable threading for subsequent chunks in this send too.
+                        thread_ts = None
+                        last_result = await self._get_client(chat_id).chat_postMessage(**kwargs)
+                    else:
+                        raise
 
             # Clear Slack Assistant status as soon as the final message is posted.
             if thread_ts:
@@ -1658,11 +1680,23 @@ class SlackAdapter(BasePlatformAdapter):
                 existing_thread = None
             return existing_thread or None
 
+        # LOCAL MODIFICATION (patches/slack-cannot-reply-to-message.patch):
+        # metadata.thread_id may be synthesized from the message's own ts
+        # purely for session keying (see the reply_in_thread=False branch
+        # above). Replying to that synthetic ts hits cannot_reply_to_message
+        # whenever the message can't host a thread (system messages, child
+        # replies, deleted, ephemeral). Detect the synthetic case (thread_id
+        # == reply_to and no real thread_ts field) and fall back to a plain
+        # channel message.
         if metadata:
-            if metadata.get("thread_id"):
-                return metadata["thread_id"]
-            if metadata.get("thread_ts"):
-                return metadata["thread_ts"]
+            thread_id = metadata.get("thread_id")
+            real_thread_ts = metadata.get("thread_ts")
+            if thread_id and thread_id == reply_to and not real_thread_ts:
+                return None
+            if thread_id:
+                return thread_id
+            if real_thread_ts:
+                return real_thread_ts
         return reply_to
 
     async def _upload_file(
@@ -1923,6 +1957,19 @@ class SlackAdapter(BasePlatformAdapter):
 
         text = content
 
+        # LOCAL MODIFICATION (patches/slack-cannot-reply-to-message.patch):
+        # 0a) Normalize * list markers to - (prevents interference with italic)
+        text = re.sub(r'^(\s*)\* ', r'\1- ', text, flags=re.MULTILINE)
+
+        # 0b) Strip backticks from inline code that contains emoji shortcodes.
+        # LLMs often wrap `:emoji: text` in backticks, but Slack won't render
+        # emoji shortcodes inside code spans.
+        text = re.sub(
+            r'`(:[a-z_]+:\s[^`]*)`',
+            r'\1',
+            text,
+        )
+
         # 1) Protect fenced code blocks (``` ... ```)
         text = re.sub(
             r"(```(?:[^\n]*\n)?[\s\S]*?```)",
