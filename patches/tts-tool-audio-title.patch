diff --git a/tools/tts_tool.py b/tools/tts_tool.py
index 3aa4875922..b4ee55f13e 100644
--- a/tools/tts_tool.py
+++ b/tools/tts_tool.py
@@ -1812,6 +1812,31 @@ def _tts_response_format_from_path(output_path: str) -> str:
 # Provider: OpenAI TTS (also used by every OpenAI-compatible TTS endpoint —
 # DeepInfra delegates here via _generate_deepinfra_tts).
 # ===========================================================================
+def _rename_with_title(path: str, title: str) -> Optional[str]:
+    """Rename a generated audio file to a human *title* (from audio-gateway's
+    X-Audio-Title header) so the Slack attachment shows a real name instead of
+    ``tts_<timestamp>``. Keeps the original extension; strips filesystem-unsafe
+    characters; falls back to a timestamp suffix on name collision. Returns the
+    new path, or ``None`` on any failure (caller keeps the original).
+    LOCAL MODIFICATION — see patches/tts-tool-audio-title.patch.
+    """
+    clean = re.sub(r'[<>:"/\\|?*\x00-\x1f]', "", title)
+    clean = re.sub(r"\s+", " ", clean).strip().rstrip(".")[:80].strip()
+    if not clean:
+        return None
+    src = Path(path)
+    target = src.with_name(f"{clean}{src.suffix}")
+    try:
+        if target.exists() and target != src:
+            stamp = datetime.datetime.now().strftime("%H%M%S")
+            target = src.with_name(f"{clean} {stamp}{src.suffix}")
+        src.rename(target)
+        return str(target)
+    except OSError as exc:
+        logger.warning("Could not rename audio to title %r: %s", clean, exc)
+        return None
+
+
 def _generate_openai_tts(
     text: str,
     output_path: str,
@@ -1823,7 +1848,7 @@ def _generate_openai_tts(
     voice: Optional[str] = None,
     speed: Optional[float] = None,
     instructions: Optional[str] = None,
-) -> str:
+) -> Optional[str]:
     """Generate audio via the OpenAI ``audio.speech.create`` SDK shape.
 
     Optional kwargs let OpenAI-compatible backends (DeepInfra etc.) reuse
@@ -1850,8 +1875,15 @@ def _generate_openai_tts(
             unaffected.
 
     Returns:
-        Path to the saved audio file.
+        A human-readable title for the audio when the server provides one
+        (audio-gateway's ``X-Audio-Title`` header, generated by its DeepSeek
+        prep step), else ``None``. Used by the caller to name the saved file /
+        Slack attachment. LOCAL MODIFICATION — see patches/tts-tool-audio-title.patch.
     """
+    # LOCAL MODIFICATION (patches/tts-tool-audio-title.patch): decode the
+    # RFC 5987 / percent-encoded X-Audio-Title response header below.
+    from urllib.parse import unquote as _unquote  # noqa: E402
+
     # Only resolve the OpenAI auth chain when the caller didn't pass explicit
     # credentials. OpenAI-compatible backends (DeepInfra) pass api_key /
     # base_url / model / voice through and never hit the managed-gateway path.
@@ -1916,10 +1948,15 @@ def _generate_openai_tts(
             create_kwargs["instructions"] = instructions
         if language:
             create_kwargs["extra_body"] = {"lang_code": language}
-        response = client.audio.speech.create(**create_kwargs)
-
-        response.stream_to_file(output_path)
-        return output_path
+        # LOCAL MODIFICATION (patches/tts-tool-audio-title.patch):
+        # with_raw_response so we can read the X-Audio-Title header alongside
+        # the binary body (the typed client drops response headers).
+        raw = client.audio.speech.with_raw_response.create(**create_kwargs)
+        with open(output_path, "wb") as fh:
+            fh.write(raw.content)
+        title_header = raw.headers.get("x-audio-title")
+        title = _unquote(title_header).strip() if title_header else None
+        return title or None
     finally:
         close = getattr(client, "close", None)
         if callable(close):
@@ -3258,6 +3295,9 @@ def _text_to_speech_single(
     # Ensure parent directory exists
     file_path.parent.mkdir(parents=True, exist_ok=True)
     file_str = str(file_path)
+    # LOCAL MODIFICATION: human title from audio-gateway's X-Audio-Title header
+    # (openai provider only). See patches/tts-tool-audio-title.patch.
+    audio_title: Optional[str] = None
 
     try:
         # Generate audio with the configured provider
@@ -3304,7 +3344,9 @@ def _text_to_speech_single(
                     "error": "OpenAI provider selected but 'openai' package not installed."
                 }, ensure_ascii=False)
             logger.info("Generating speech with OpenAI TTS...")
-            _generate_openai_tts(text, file_str, tts_config, instructions=instructions)
+            audio_title = _generate_openai_tts(
+                text, file_str, tts_config, instructions=instructions,
+            )
 
         elif provider == "deepinfra":
             try:
@@ -3460,6 +3502,17 @@ def _text_to_speech_single(
         elif provider in {"elevenlabs", "openai", "mistral", "gemini"}:
             voice_compatible = want_opus and file_str.endswith(".ogg")
 
+        # LOCAL MODIFICATION: rename the auto-named tts_<timestamp> file to the
+        # audio-gateway title so the Slack attachment shows a real name. Applies
+        # when the caller didn't pin an output_path, and to the gateway's own
+        # auto voice-reply temp files (tts_reply_<uuid>, build_auto_tts_output_path)
+        # — those callers read the returned file_path, so a rename is honoured.
+        # See patches/tts-tool-audio-title.patch.
+        if audio_title and (not output_path or os.path.basename(str(output_path)).startswith("tts_reply_")):
+            _renamed = _rename_with_title(file_str, audio_title)
+            if _renamed:
+                file_str = _renamed
+
         file_size = os.path.getsize(file_str)
         logger.info("TTS audio saved: %s (%s bytes, provider: %s)", file_str, f"{file_size:,}", provider)
 
