diff --git a/agent/auxiliary_client.py b/agent/auxiliary_client.py
index 4c8877232..339909504 100644
--- a/agent/auxiliary_client.py
+++ b/agent/auxiliary_client.py
@@ -3418,7 +3418,17 @@ def resolve_provider_client(
     # ── Custom endpoint (OPENAI_BASE_URL + OPENAI_API_KEY) ───────────
     if provider == "custom":
         if explicit_base_url:
-            custom_base = _to_openai_base_url(explicit_base_url).strip()
+            # Respect api_mode=anthropic_messages: without this guard the
+            # /anthropic suffix gets rewritten to /v1, and although
+            # _maybe_wrap_anthropic still detects the Anthropic surface via
+            # api_mode, build_anthropic_client would be handed the rewritten
+            # /v1 base and target /v1/messages on an /anthropic-only gateway
+            # → 404 "Endpoint not found". Local-modification: see
+            # patches/auxiliary-client-anthropic-mode-respect.patch.
+            if api_mode == "anthropic_messages":
+                custom_base = (explicit_base_url or "").strip().rstrip("/")
+            else:
+                custom_base = _to_openai_base_url(explicit_base_url).strip()
             custom_key = (
                 (explicit_api_key or "").strip()
                 or os.getenv("OPENAI_API_KEY", "").strip()
