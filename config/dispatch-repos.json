{
  "_comment": [
    "Repo allowlist for scripts/hermes-cc.sh. ABSENCE IS A DENIAL — hermes-cc.sh accepts a",
    "repo NAME from this file and resolves the path itself, so the agent never names a",
    "filesystem path and cannot reach a repo that is not listed here.",
    "",
    "maxTier caps what a dispatch into that repo may do, independently of what the caller",
    "asks for. 'investigate' is read-only and cannot lose anything; 'author' may open a",
    "GitHub issue; 'implement' may create a branch and a PR (never a merge, never a push to",
    "a default branch). A repo's tier is the ceiling, not the default.",
    "",
    "DELIBERATELY ABSENT, and staying absent:",
    "  dotfiles-private, homelab-private — private by construction. Their whole point is",
    "    that their contents are not referenced from anywhere else; an agent-readable",
    "    episode inside them would undo that.",
    "  brain — the personal vault. Read-only would be technically safe, but journal and",
    "    personal notes are not triage material, and the obsidian skill already owns that",
    "    door with conventions this tool does not implement.",
    "",
    "Everything here is currently capped at 'investigate' because the author and implement",
    "tiers are not built yet (Phase 4). Raising a ceiling is a deliberate, reviewed edit."
  ],
  "repos": {
    "hermes-agent": { "path": "~/SourceRoot/hermes-agent", "maxTier": "investigate" },
    "sideclaw": { "path": "~/SourceRoot/sideclaw", "maxTier": "investigate" },
    "homelab": { "path": "~/SourceRoot/homelab", "maxTier": "investigate" },
    "vps": { "path": "~/SourceRoot/vps", "maxTier": "investigate" },
    "argo": { "path": "~/SourceRoot/argo", "maxTier": "investigate" },
    "dotfiles": { "path": "~/SourceRoot/dotfiles", "maxTier": "investigate" },
    "usage-tracker": { "path": "~/SourceRoot/usage-tracker", "maxTier": "investigate" },
    "audio-gateway": { "path": "~/SourceRoot/audio-gateway", "maxTier": "investigate" },
    "research-gateway": { "path": "~/SourceRoot/research-gateway", "maxTier": "investigate" },
    "image-share": { "path": "~/SourceRoot/image-share", "maxTier": "investigate" },
    "image-gen": { "path": "~/SourceRoot/image-gen", "maxTier": "investigate" },
    "basalt-ui": { "path": "~/SourceRoot/basalt-ui", "maxTier": "investigate" },
    "modelpick": { "path": "~/SourceRoot/modelpick", "maxTier": "investigate" },
    "rollhook": { "path": "~/SourceRoot/rollhook", "maxTier": "investigate" },
    "rollhook-action": { "path": "~/SourceRoot/rollhook-action", "maxTier": "investigate" },
    "free-planning-poker": { "path": "~/SourceRoot/free-planning-poker", "maxTier": "investigate" },
    "rb": { "path": "~/SourceRoot/rb", "maxTier": "investigate" },
    "jkrumm.com": { "path": "~/SourceRoot/jkrumm.com", "maxTier": "investigate" },
    "bun-email-api": { "path": "~/SourceRoot/bun-email-api", "maxTier": "investigate" },
    "ticktick-raycast": { "path": "~/SourceRoot/ticktick-raycast", "maxTier": "investigate" },
    "clawbar": { "path": "~/SourceRoot/clawbar", "maxTier": "investigate" },
    "kobo-mods": { "path": "~/SourceRoot/kobo-mods", "maxTier": "investigate" },
    "sy-serendipity": { "path": "~/SourceRoot/sy-serendipity", "maxTier": "investigate" }
  }
}
